import {FirstCapLetterPipe} from './first-cap-letter.pipe';

describe('FirstCapLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstCapLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
